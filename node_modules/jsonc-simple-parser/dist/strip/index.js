/* IMPORT */
import Context from '../tokenize/context.js';
import parser from './parser.js';
/* MAIN */
const strip = (text) => {
    Context.init();
    return parser(text);
};
/* EXPORT */
export default strip;
