/* IMPORT */
import parse from './parse.js';
/* MAIN */
const validate = (text) => {
    try {
        parse(text);
        return true;
    }
    catch {
        return false;
    }
};
/* EXPORT */
export default validate;
