"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveConditionalValue = void 0;
/**
 * find the first match for string, import, node, or default
 * at this point, we know we're on the right subpath already
 */
const resolveConditionalValue = (exp, options) => {
    if (exp === null || typeof exp === 'string')
        return exp;
    if (Array.isArray(exp)) {
        for (const e of exp) {
            const r = (0, exports.resolveConditionalValue)(e, options);
            if (r)
                return r;
        }
        return null;
    }
    const conditions = new Set(['default']);
    for (const condition of options.conditions ?? ['import', 'node']) {
        if (condition.startsWith('!')) {
            conditions.delete(condition.slice(1));
        }
        else {
            conditions.add(condition);
        }
    }
    for (const [k, v] of Object.entries(exp)) {
        if (conditions.has(k)) {
            return (0, exports.resolveConditionalValue)(v, options);
        }
    }
    return null;
};
exports.resolveConditionalValue = resolveConditionalValue;
//# sourceMappingURL=resolve-conditional-value.js.map